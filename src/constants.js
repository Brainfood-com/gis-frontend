import {
  List as imList,
  Map as imMap,
  OrderedMap as imOrderedMap,
} from 'immutable'

export const immutableEmptyList = imList()
export const immutableEmptyMap = imMap()
export const immutableEmptyOrderedMap = imOrderedMap()

